# Cloudflare Pages handles SPA routing natively.
# The /* → /index.html rule is NOT needed and causes an infinite loop error.
# Leave this file as-is — Pages will serve index.html for all unknown routes automatically.
